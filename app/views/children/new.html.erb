<h1>New Child</h1>

<h1>Create a Child</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@child) do |f| %>
      <%= f.label :name %>
      <%= f.text_field :name %>

      <%= f.hidden_field :parent_id, value: session[:parent_id] %>

      <%= f.submit "Create child account", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
